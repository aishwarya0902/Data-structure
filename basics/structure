
#include <iostream>
#include <stdio.h>
using namespace std;

struct rectangle    // this is known as definition
{
    int length;
    int breadth;

} r3,r4; // we can declare the as a global variable also

struct rectangle r2; // we can also declare and initialize the struct variable outside main 

int main()
{
    struct rectangle r1; // it is an declaration

}
